import { ScrollFade } from './scroll-fade';

describe('ScrollFade', () => {
  it('should create an instance', () => {
    const directive = new ScrollFade();
    expect(directive).toBeTruthy();
  });
});
