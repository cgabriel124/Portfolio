import { MouseOver } from './mouse-over';

describe('MouseOver', () => {
  it('should create an instance', () => {
    const directive = new MouseOver();
    expect(directive).toBeTruthy();
  });
});
